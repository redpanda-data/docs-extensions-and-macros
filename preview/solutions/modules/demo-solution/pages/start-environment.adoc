= Start the environment
:page-layout: solution-step
:description: Start Redpanda and the demo services with Docker Compose.
:page-solution-step-duration: 5

== What you do

Download link:{attachmentsdir}/docker-compose.yml[docker-compose.yml] and start the stack.

[source,bash]
----
include::example$commands.sh[tags=up]
----

== Why

Everything the solution needs runs locally; nothing is installed on the host.

== Verify

[source,bash]
----
include::example$commands.sh[tags=info]
----

The output lists one broker.

== In production

Use the Helm chart or Redpanda Cloud instead of Compose.
